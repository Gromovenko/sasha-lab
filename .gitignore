node_modules/
*.log
dist/
seo/.env
secrets/

# Вопросы посетителей и снимки поисковых данных: личные контакты и IP — не в git
seo/data/*.json

# Сбор базы знаний: кэш чужих страниц и отладочная выгрузка без БД — не в git
.harvest-cache/
.harvest-out/
