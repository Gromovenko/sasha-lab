# Сколько занимает полный парсинг и чем его ускорить

Оценка снята 19.09.2026 на сервере проекта (77.105.168.153, 2 ядра, 3,9 ГБ).
Исходники цифр: замер `node harvest/run.js probe` → `seo/data/harvest-speed.json`
(10:45 UTC), фактическое число собранных страниц — из БД `documents`,
фактический темп — гистограмма `fetched_at` по минутам живого захода
steklafar.ru: **ровно 60 страниц в минуту** при паузе 1000 мс.

## Как считается время (важно: план завышает)

`harvest/http.js → get()` ставит отметку `lastHit` **перед** запросом и ждёт
`пауза − (сейчас − lastHit)`. То есть шаг = **max(пауза, время ответа)**, время
ответа прячется внутри паузы. А `probe.js` пишет в план `perPageMs = пауза + ответ`
— это ошибка модели на 13 часов по всему заходу (95 ч «по плану» против 82 ч
фактических). Колонка «план» ниже оставлена, чтобы расхождение было видно.

## Таблица: полный заход с текущего состояния базы

| Источник | Осталось стр. | Пауза, мс | Ответ, мс | По плану, ч | **Факт, ч** |
|---|---:|---:|---:|---:|---:|
| bi-vision.ru | 3 300 | 40 000 | — | 36,7 | **36,7** |
| nts-auto.com | 3 100 | 20 000 | — | 17,2 | **17,2** |
| electro-kot.ru | 30 000 | 1 000 | 340 | 11,2 | **8,3** |
| www.xenonshop.ru | 2 691 | 1 000 | 4 321 | 4,0 | **3,2** |
| aozoom-light.ru | 10 995 | 1 000 | 637 | 5,0 | **3,1** |
| www.criline.ru | 9 740 | 1 000 | 774 | 4,8 | **2,7** |
| vdf-light.ru | 8 285 | 1 000 | 369 | 3,2 | **2,3** |
| luxsar.ru | 7 040 | 1 000 | 380 | 2,7 | **2,0** |
| dixel.store | 4 800 | 1 000 | 1 023 | 2,7 | **1,4** |
| tuningfar.com | 3 700 | 1 000 | 204 | 1,2 | **1,0** |
| optima-light.ru | 3 450 | 1 000 | 111 | 1,1 | **1,0** |
| www.galogenu.net | 3 291 | 1 000 | 336 | 1,2 | **0,9** |
| legal-xenon.ru | 3 140 | 1 000 | 551 | 1,4 | **0,9** |
| statlight.ru | 2 650 | 1 000 | 838 | 1,4 | **0,7** |
| mtflight-shop.com | 1 400 | 1 000 | 1 041 | 0,8 | **0,4** |
| steklafar.ru | 1 142 | 1 000 | 140 | 0,4 | **0,3** |
| autosvet.pro / hltuning.ru | 163 | 1 000 | — | 0,1 | **0,1** |
| www.hidplanet.com (titles) | 0 | 10 000 | — | 0 | **0** |
| **ИТОГО** | **98 887** | | | **94,8** | **82,1** |

Последовательно (текущий режим `HARVEST_PAR=1`) — **82 часа ≈ 3,5 суток**.

Главная диспропорция: **bi-vision + nts-auto = 54 ч из 82 (66% времени) на
6 400 страниц (6,5% объёма)**. Это не наша медлительность, а `Crawl-delay`,
названный самими площадками (40 и 20 с). Быстрее ходить нельзя — можно только
убрать их с критического пути.

## Ускорение: что сколько даёт

| Мера | Стало, ч | Выигрыш | Цена/риск |
|---|---:|---:|---|
| как сейчас, PAR=1 | 82 | — | — |
| **PAR=2** | 41 | −41 ч | 2 процесса × heap; на 3,9 ГБ безопасно |
| **PAR=3** | 36,7 | −45 ч | упёрлись в bi-vision; heap снизить до 768 МБ |
| PAR=4 и выше | 36,7 | 0 | бессмысленно — пол задан чужим robots |
| **два медленных источника отдельным фоновым процессом** + PAR=3 на остальных | 9,4 (полезная часть) / 36,7 (хвост) | вся база кроме двух — **за смену** | процессов 4, но два из них спят 95% времени |
| titles-режим для bi-vision/nts-auto (как у hidplanet: карта сайта вместо страниц) | минуты | −54 ч | вместо текста — только номенклатура из URL |
| keep-alive агент в `http.raw` | 80,4 | −1,7 ч | помогает только там, где ответ > паузы (xenonshop 4,3 с, dixel и mtflight ~1 с); заодно снимает TLS-рукопожатие с чужого сервера на каждой странице |
| сузить `include`/канонизацию URL (пагинация, `?sort=`, `?utm`, дубли) | пропорционально | 10–20% объёма | нужен разбор карт сайта по источникам |
| conditional GET (ETag/If-Modified-Since) | 0 | 0 по времени | пауза остаётся; экономит трафик и CPU, не часы |
| HTTP/2, больше соединений к одному хосту | 0 | 0 | узкое место — пауза вежливости, а не канал |

Рекомендуемая связка, без единой новой строки риска для источников:

```bash
# 1) быстрые источники в три потока
HARVEST_PAR=3 HARVEST_HEAP_MB=768 scripts/harvest-queue.sh --plan
# 2) два «медленных по robots» — отдельно и заранее, они не мешают никому
nohup node harvest/run.js crawl bi-vision.ru &
nohup node harvest/run.js crawl nts-auto.com &
```

Оговорка: `harvest-queue.sh` сейчас режет `HARVEST_PAR` потолком 2 и держит
один flock на сервер — для схемы выше потолок надо поднять до 3, а медленные
источники вынести из-под общего замка (или дать им свой).

## Что стоит поправить в коде

1. `harvest/probe.js`: `perPageMs = Math.max(recommend, latency)` вместо суммы —
   иначе `etaHours` и порядок `--plan` считаются по неверной модели.
2. `harvest/http.js`: общий `https.Agent({ keepAlive: true })`.
3. `scripts/harvest-queue.sh`: потолок `PAR` 2 → 3, отдельная дорожка для
   источников с `delayMs >= 10000`.
