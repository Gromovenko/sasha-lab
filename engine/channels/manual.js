// Ручной канал — не заглушка, а рабочий режим. Пока у Авито и WhatsApp нет
// ключей (и для площадок, где API нет вовсе), движок всё равно доводит работу
// до конца: готовый текст лежит в карточке, мастер жмёт «скопировать» и
// вставляет в чат площадки. Отправка при этом ЗАПИСЫВАЕТСЯ — иначе сделка
// повиснет в «ждёт ответа» и следом уйдёт лишнее напоминание.
const id = 'manual';
const title = 'Вручную (копипаста)';
const configured = () => true;
const send = async () => ({ ok: true, manual: true, id: null });
module.exports = { id, title, configured, send, inbound: false };
